# Dependencies
/node_modules

# Production
/build

# Generated files
.docusaurus
.cache-loader

# .NET API reference intermediates — regenerate with `npm run docs:api`.
# (The generated Markdown in /api-docs IS committed so CI can build without .NET.)
/api-bin
/api
# EN mirror of /api-docs — recreated from it before every build (sync-api-en.mjs).
/api-docs-en

# Misc
.DS_Store
.env.local
.env.development.local
.env.test.local
.env.production.local

npm-debug.log*
yarn-debug.log*
yarn-error.log*

.vs/
